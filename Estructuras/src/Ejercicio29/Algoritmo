1.- Meta:
    Para ilustrar la inserción de nodos al principio de una lista simplemente
    ligada usan domo Nodo un IMC pueden leerse los datos de archivo o
    capturarlos por centinela. La salida puede ser elegible a pantalla o archivo

2.- Datos:
    2.1.- captura()
        Se capturan datos: nombre, peso,y talla de varias personas usando
        datos centinela para terminar

        cuenta = 1;
        MySimpleLinkedList lsl = nueva MySimpleLinkedList();
        centinela = "fin";
        Escribe("Deme el nombre de la persona" + cuenta + ":");
        Escribe("o \"fin\" para terminar: "); nombre = ?
        Mientras (nombre != "fin")) empezar
            Escribe("cuanto pesa la persona" + cuenta + ": "); peso = ?
            Escribe("la talla de la persona" + cuenta + "es : "); talla = ?
            cuenta++;
            imcp = nuevo IMC(nombre, peso, talla);
            lsl.insert(imcp);
            Escribe("Deme el nombre de la persona" + cuenta);
            Escribe("o \"fin\" para terminar"); nombre = ?
        terminar

    2.2.- lectura()
        Se leen nombre, peso y talla de varias personas
        de un archivo en el disco cuyo nombre deberá saber el usuario
        en caso de no saberlo se presentara la lista de archivo

            2.2.2 pideArchivo()
                Escribir("Deme nombre de archivo a leer: "); archivo = ?
                Si (arhcivo == null || archivo == "") entonces
                    ListaFicheros();
                Sino
                    datosIn(archivo);
                terminar

    2.3.- datosIn(archivo) : int // entrada por archivo
        cuanta = 0;
        in = abrir(arhicvo);
        Mientras (in.leer() != EOF) empezar
            cuenta++;
            nombre = in.nextLine();
            peso = toDouble(in.nextLine());
            talla = toDouble(in.nextLine());
            imcp = nuevo IMC(nombre, peso, talla);
            lsl.insert(imcp);
        terminar
        in.close()
        regresar cuenta al llamador

3.- Calculos:
    3.1.- calcImc(imcp)
        peso = imcp.getPeso();
        talla = imcp.getTalla();
        if (talla != 0)
                imc = peso / (talla * talla);
        regresar imc calculado al llamador

    3.2.- insert(lsl, imc)      // debe implementarse en el ADT
        q = nuevo IMC();
        q.setNext(lsl);
        lsl = q;

4.- Resultados:
    4.1.- Desplegar la lista de nodos insertados en la misma
        // Se imprime una tablita para dar formato
        cuenta = 1;
        temp = lsl;
        Mientras (temp != null) empezar
            // Se imprimen los getters
            Escribe(cuenta + " " + temp.info.getNombre());
            Escribe(" " + temp.info.getPeso());
            Escribe(" " + temp.info.getTalla());
            temp = temp.getNext();
        terminar

    4.2.- Guardar la información de la lista en el disco
        Escribe("Deme nombrer del arhcivo: "); arch = ?
        Si (existe(arch))
            datosOut(arch, lsl);
        de otro modo ListaFicheros("."); // método auxiliar

    4.3.- datosOut(arch, lsl)
        out = abrir(arch)
        temp = lsl
        Mientras (temp != null) empezar
            Escribe(arch, temp.info.getNombre());
            Escribe(arch, temp.info.getPeso());
            Escribe(arch, temp.info.getTalla());
            temp = temp.getNext();
        terminar
        Esribe("Se escribieron los datos en: " + file);
        close(arch);        // Cierra el archivo de salida

5.- Navegabilidad:
    5.1.- Principal
        Repetir empezar
            Escribir("Menú principal: ");
            Escribir("1.- Descripción del programa");
            Escribir("2.- Entrada de datos");
            Escribir("3.- Resultados");
            Escribir("4.- Ayuda");
            Escribir("5.- Salir");
            Escribir("su opción:");
            opcion = ?
            Según (opcion) empezar
                caso 1: Inicio(); romper;
                caso 2: entradas(); romper;
                caso 3: salidas(); romper;
                caso 4: ayuda(); romper;
                dafault: escribir("Opciones validas 1-5");
            terminar
        terminar mientras (opcion != 5);

    5.2.- submenu de entradas()
        Repetir empezar
            Escribir("calculo del indice de masa corporal");
            Escribir("1.- por teclado");
            Escribir("2.- por archivo");
            Escribir("3.- Regresar al menú principal");
            Escribir("su eleccion: ");
            entrada = ?
            Según (entrada) empezar
                caso 1: captura(); romper;
                caso 2: lectura(); romper;
                omisión: Escribir("opciones validas 1-3");
            terminar
        terminar Mientras(entrada != 3);

    5.3.- Ayuda()
        Escribir("Al dar enter se presentan los archivo almacenados en");
        Escribir("el directorio de trabajo actual");

    5.4.- Salidas()
        opción = 0;
        Repetir empezar
            Escribe("Salidas: indice de masa corporal");
            Escribe("1.- por pantalla");
            Escribe("2.- por archivo");
            Escribe("3.- regresar al principal");
            Escribe("su elección:"); opcion = ?
            Según (opcion) empezar
                caso 1: resultados(4.1); romper
                caso 2: guardar(4.2); romper;
                omisión: Escribe opciones validas
            terminar
        terminar Mientras (opcion != 3);
