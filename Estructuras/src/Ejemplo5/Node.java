package Ejemplo5;

public class NOde {
    private Object info;
    private Node next;

    public Node() {
        info = null;
        next = null;
    }

    public Node(Object valor) { this(valor, null); }

    public Node(Object o, Node n) {
        setInfo(o);
        setNext(n);
    }

    setInfo
    setNext
    getInfo
    getNext
}
